package com.iloo.javasolid.core;

/**
 * The Animal interface represents a general animal. It adheres to the Single
 * Responsibility Principle (SRP) as it defines only the common behavior of
 * animals.
 */
interface Animal {
	String getName();
}
